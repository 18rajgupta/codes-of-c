//write a c program to enter length and bredth of a rectangle and find its perimeter.

#include <stdio.h>
int main()
{
	int l,b,p;
	l=20;
	b=30;
	p=2*(l+b);
	printf("Perimeter of a rectange=%d",p);
	return 0;
 } 
